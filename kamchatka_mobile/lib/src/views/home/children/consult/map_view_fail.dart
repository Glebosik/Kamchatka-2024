import 'package:flutter/material.dart';

class MapViewFail extends StatelessWidget {
  const MapViewFail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
