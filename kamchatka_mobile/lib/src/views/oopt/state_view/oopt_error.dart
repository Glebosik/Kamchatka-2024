import 'package:flutter/material.dart';

class OoptErrorView extends StatelessWidget {
  const OoptErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
