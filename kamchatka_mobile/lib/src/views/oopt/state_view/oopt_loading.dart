import 'package:flutter/material.dart';

class OoptLoadingView extends StatelessWidget {
  const OoptLoadingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
